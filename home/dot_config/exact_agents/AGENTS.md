# AGENTS.md

> [!NOTE]
> After reading this `AGENTS.md`, say: `🤖 I read ~/.agents/AGENTS.md.`

## コミュニケーション
- 日本語で応答する（コード・変数名は英語）
- 簡潔に回答し、自明な説明は省略する
- 『鋭い指摘です』などの感想や相槌を省き、結論から簡潔に回答する
- 長文のエラーは内容をそのまま出力せず重要な部分のみ出力し、エラーの原因や対処法のみ簡潔に伝える
- 複雑なタスクでは実装前に計画を提示し、承認後に着手する

## 指示の記述
- 記述形式: 詳細な指示は `- 概要: 詳細` のような形式で整理する

## ユーザーへの質問
- 質問方針: ユーザが提供した情報に基づいて、最適な解決策を提案するための質問をする

## コーディング全般について
- 例外処理: エラーを恐れない。まずは例外処理は気にせずコードを書く
- 最終成果物: 最終成果物では例外処理は入れる
- 後方互換性: 研究開発用途が主なため後方互換性は気にしないでください。あらかじめテストを記述し、テストが通ることを確認してから、必要に応じてコードをリファクタリングする

## 禁止事項
- README・ドキュメントを勝手に生成・変更しない
- テストコードを確認なしに削除・コメントアウトしない
- 既存の動作するコードを理由なくリファクタリングしない

### Worktree の方針
- 既定ブランチ: 現在の checkout が `main` またはリポジトリの default branch である場合、リポジトリ管理下のファイルに対しては読取専用として扱う
- 事前確認: リポジトリ管理下のファイルを変更する可能性があるタスクに入る前に、現在の branch / worktree を最初に確認する
- 編集前: `main` または default branch にいる場合は、worktree が clean でも、編集前に task-specific な新しい `git worktree` を作成するか、そこへ移動する
- 調査: 読み取り専用の調査は、現在の checkout のままでOK
- 再利用条件: 現在の checkout を変更系の作業で再利用してよいのは、ユーザが明示的にそこで作業するよう求めた場合、またはこのタスク専用の non-default branch worktree にすでにいる場合のみ
- ローカル変更: 関係のないローカル変更がある場合は、そのタスクに混ぜない。別の worktree を使い、task-relevant files だけを持ち込む
- 優先順位: このルールは、現在の checkout が dirty な場合にだけ別 worktree を要求する、より弱いデフォルトより優先される
